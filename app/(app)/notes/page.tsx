import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function NotesPage() {
  return (
    <SectionPlaceholder
      title="Notes"
      description="Markdown notes per subject."
    />
  );
}
